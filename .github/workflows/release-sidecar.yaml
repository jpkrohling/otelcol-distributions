name: Release sidecar

on:
  workflow_run:
    workflows: [Continuous Integration - sidecar]
    types:
      - completed

jobs:
  release:
    name: Release sidecar
    uses: ./.github/workflows/base-release.yaml
    with:
      distribution: sidecar
    secrets: inherit
